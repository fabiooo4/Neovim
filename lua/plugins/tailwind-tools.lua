return {
  -- Uses lspconfig deprecated LSP API
  -- "luckasRanarison/tailwind-tools.nvim",
  -- dependencies = { "nvim-treesitter/nvim-treesitter" },
  -- opts = {
  --   conceal = {
  --     enabled = false,
  --     min_length = 60,
  --   }
  -- }
}
